# Themix-managed files (theme-specific, vary per machine)
# nvim colorschemes (dirs under lua/ are always themix; real modules are flat .lua files)
nvim/colors/
nvim/lua/*/

# Theme symlink + generated theme dirs
themes/*/
!themes/scripts/
!themes/templates/
themes/current

# Kitty theme (symlink managed by themix)
kitty/theme.conf

# Hypr theme files (managed by themix)
hypr/theme.conf
hypr/hyprlock-theme.conf
hypr/config/environment.conf

# Niri theme (symlink managed by themix)
niri/theme.kdl

# Niri config (symlinked per-machine by install.sh)
niri/config.kdl

# Yazi theme (copy managed by themix)
yazi/theme.toml

# Nushell runtime files
nushell/history.*
nushell/zoxide.nu
